<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Màu nền xám đậm nhẹ -->
    <solid android:color="#1E1E1E" />

    <!-- Bo góc -->
    <corners android:radius="12dp" />

    <!-- Padding nội bộ nếu cần -->
    <padding
        android:left="12dp"
        android:top="12dp"
        android:right="12dp"
        android:bottom="12dp" />

</shape>
